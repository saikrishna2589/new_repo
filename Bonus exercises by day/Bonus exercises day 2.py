# we will build program which checks the title of the book and gives the length of a title
title_test = "Enter a title: "  # string assignment to variable
title_input = input(title_test)  # storing output of the user input in variable title_input
print("Title length is " + str(len(title_input)))  # printing the title length of output of the
# user input,which is stored in title_input.len() is a function. each function 
